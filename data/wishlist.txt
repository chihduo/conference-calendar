# One acronym per line. The nightly job expands each into data/conferences/<id>.yml
# via the discovery cascade and removes the line; anything it could not resolve
# stays here annotated with why.
#
# Add a line here to add a conference. Everything below is a comment.
